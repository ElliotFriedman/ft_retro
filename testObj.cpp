#include "LivingObject.hpp"
#include "Enemy.hpp"
#include "Bullet.hpp"
#include "Player.hpp"
#include <array>

int main()
{
//	BulletObject a(false, 'B', 10, 10, 10, 10);
//	template<typename T> BulletObject(false, 'B', 10, 10, 10, 10);
	//BulletObject *a = new BulletObject(false, 'B', 10, 10, 10, 10);
	std::array<BulletObject(false, 'B', 10, 10, 10, 10), 10> allBullets;
	int i = 0;
	while (i < 10)
	{
		allBullets[i] = 1;
		i++;
	}
}

